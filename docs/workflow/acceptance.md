# 上线与验收状态

2026-09-22。用户明确授权实现和 GitHub 部署，发布账号 web3kk0713-hue。

## 已完成

- TypeScript 检查、生产构建及自动测试通过；Linux GitHub Actions 完成真实构建和 Pages 发布。
- 官方源实测覆盖 568 个合约、526 个资产，单轮约 7–8 秒；逐合约 OI 数量×标记价×真实美元汇率的聚合核验通过。覆盖率会随新上市、缺失供应量、过期源时间、网络与限流变化。
- 验收中发现并修复同名币错配：不再跨平台按 slug/symbol 自动授权；供应源价格仅是拒绝异常映射的附加检查，不作为身份的唯一证明。
- 浏览器真实行情、搜索、时间范围、来源字段、强提醒测试弹窗、390px 移动布局验证；未用模拟行情填充线上页面。
- 真实 Node 后台采集并写入 SQLite；终止进程后重启，采集前即可读回原有快照和3个BTC分钟历史点；后台API与前端真实联通。
- 公式、倍率、缺失/过期、跨级告警与冷却、供应源失败与取消、映射污染、数据库事务/租约/保留期、HTTP输入和推送目标限制有自动回归。

## 明确未上线或未实测

- GitHub Pages 只发布静态前端。网站默认浏览器直连，关闭网页或设备休眠后无法保证分钟采集与通知。未部署外部7×24后台、PostgreSQL或VAPID推送。
- PostgreSQL/Docker部署配置已提供；本机没有对应运行环境，因此未宣称完成该生产路径验收。
- 页内强提醒已实测；系统通知代码已实现，但当前自动化浏览器未完成OS授权/送达验收，真实Web Push送达也未实测。用户需在受支持浏览器中主动授予通知权限。
- 来源JSON导出功能已实现；内嵌浏览器未提供可验证的下载完成事件，验收仅确认源字段可查看。
- 30天是实际采集数据的保留窗口，不是预填的30天历史；未完成30天持续运行与满容量压测。

## 3 / 7 天变化与设计修正（2026-09-22 后续验收）

- 自动测试57项通过，含3天/7天完整分钟序列比较、共同基准、零OI、缺失/过期/未来数据、短历史、曲线断点、供应量2小时边界及极小变化格式。TypeScript与生产构建通过；ECharts懒加载包仍有体积告警，不影响构建。
- SQLite验证旧表无损加列、新旧历史区别、跨分钟耗时抖动、3/7天查询与重启后旧FDV不重算。新版本按 startedAt 分桶，旧版缺少有效性标记的估值不再作为已验证历史。
- 本机真实Node进程实测：18:17轮耗时7523ms，526币OI、380流通市值、297FDV，1个合约源数据不完整。BTC分钟点写入SQLite后终止进程，再以禁用立即采集的配置重启，历史原值与时间完全恢复。该证据不是云端部署或PostgreSQL验收。
- 浏览器真实采集从18:12起积累，至18:21有9个OI/FDV共同有效分钟点；刷新后保留起点与历史。已点验3天、7天、涨跌幅、金额切换；不足7天明示，不用测试数据填充生产。
- 浅色布局和390px/768px响应式截图检查通过，无页面横向溢出；强提醒测试弹窗在390px可读。修正极小变化被舍入为负零、图表重复刻度，以及历史说明不分行的问题。系统通知送达仍未验收。

## 后续运行与回退

网站推送 main 后自动发布，失败构建不覆盖上一成功版本。需要回退时从已验证提交创建恢复提交再发布，不改写公开历史。后台部署前须配置允许来源、HTTPS、数据库备份及稳定VAPID密钥，并在目标地区验证官方API连通性。

本轮本地开发/后台测试进程已结束，不安装机器级计划任务或系统服务。测试数据库和原始探测输出位于被Git忽略的 output/，不随网站公开。
