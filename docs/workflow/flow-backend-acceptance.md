# 订单流后台本机验收 — 2026-09-23

状态：实现与本机持久化/重启验收通过；以下限制仍然适用。临时后台已关闭，不代表云主机部署或 7×24 服务已上线。

## 环境与复现

- 地址：`http://127.0.0.1:8793`；仅临时本机进程，未安装服务、修改防火墙或读取 `.env`。
- 独立 SQLite：`D:\CodexProjects\binance-oi-monitor\data\flow-acceptance-20260923-180744.sqlite`，保留未删除，已被 Git 忽略；最终大小 104,255,488 字节。
- 启动：`node --import tsx server/flow-acceptance.ts 20260923-180744`。
- 不采集重启：上述命令末尾增加 `read-only`，即 `startJobs:false`；通过标准输入 `stop` 优雅退出。
- 本文时间均为北京时间（UTC+8）；观测使用真实币安公开数据，不使用测试样例代替在线结果。

## 回归结果

`node node_modules/vitest/vitest.mjs run tests/flow-server-storage.test.ts tests/flow-server-api.test.ts tests/server-api.test.ts tests/server-storage.test.ts`

2026-09-23 18:25：4 个文件、38 项测试全部通过。覆盖真实 SQLite 重启、7 天保留边界、完整 10,081 点分钟窗口、原有表不变、空值/原始十进制字符串、事件基础证据不可变、结果追加、严格 as-of、双连接租约、防止重复采集、最终异步写入后关闭、关闭异常释放租约、URI/Unicode 验证和限流。

- `hours=0.5` 返回 1,800,000 毫秒窗口；允许范围 `1/60..168` 小时。
- 历史接口 30 次/分钟，第 31 次返回 429。回归发现并修复了旧的限流响应未附 `statusCode`、被统一错误处理转成 500 的问题。
- 测试确认 `startJobs:false` 不构造行情 feed、不调用源请求；读历史不会改变订阅。
- `git diff --check` 通过。

## 实际采集与目录验证

| 阶段 | 本机时间 | 实际结果 |
| --- | --- | --- |
| 首次运行 | 18:07:42—18:19:34 | OI 多轮推进；旧目录 549 个市场、13/13 连接，预热后 549 ready；产生真实大单、买卖压力及突破事件。旧目录后续发现 Unicode/单字符遗漏，不能作为最终全量证明。 |
| 当前分钟/关闭修复重启 | 18:19:42—18:28:20 | 同库历史可读；`closed:false` 的当前 BTC 分钟蜡烛持续落库；旧目录预热后 549 ready。 |
| 最终目录修复重启 | 18:28:22—18:30:21 | 569 个市场 = 568 个合约 + 1 个默认现货，合约符号与 OI 的 568 合约集合比较，missing/extra 均为空；13/13 连接。 |
| 不采集重启 | 18:30:47—18:31:50 | 569 行全部 disconnected，0 外部已建立 TCP 连接，OI 时间不再推进；历史及事件可读。 |

最终版本实际收到了 `币安人生USDT`、`我踏马来了USDT`、`龙虾USDT`、`牛来USDT`、`哈基米USDT` 的价格、成交和 K 线时间；单字符 base、U 报价币纳入官方目录校验。

最终运行两次新 OI 快照 `asOf`：`1790159336157` → `1790159365318`，间隔 **29,161 ms**。第一轮覆盖 526 个资产、1 个合约缺失；第二轮覆盖 380 个资产、150 个合约缺失，不能把目录完整等同于观测值全部完整。

实际半小时历史接口在 18:20:33 返回 30 根 BTC 蜡烛，其中当前蜡烛 `closed:false`、`source:stream`、`sourceTime=1790158830866`、`receivedAt=1790158832098`，保留源时间与收到时间。

## 不前视与持久化证据

真实事件 `flow-v1:futures:ARXUSDT:sell_pressure:1790158139999` 的 1 分钟结果 `availableAt=1790158264085`。以此时间减 1 毫秒回放，结果数组为空；以此时间回放，恰好出现该 1 分钟结果，保留 `entryPrice=0.2161`、`entryAt=1790158199999`。查询未返回任何 `receivedAt>to` 蜡烛或 `detectedAt>to` 事件。

读写进程停止后的 18:30:24，与只读重启后的 18:31:38，按各表主键排序，将每一行 JSON 加换行后计算 SHA-256。下列 **全部行数与全量哈希完全一致**：

| 表 | 行数 | SHA-256 |
| --- | ---: | --- |
| flow_markets | 569 | `2fb70c387a7df025020719d61f7bc805832e1db1d049e6b651800f647f345cec` |
| flow_candles | 211,431 | `744504e0249b127f846f08f54c56e916782aaa9fafbf0edaa4f142adabca7dec` |
| flow_events | 260 | `4820c39d96e578e605ad9b85b2c73a0217988cd76363b9bc77a628037b829257` |
| flow_outcomes | 241 | `4ab3ec6a113b2c91a9e4ce7fb60a1cc6e31eb38367e26f01201c5308e3c3633f` |
| flow_depth | 1,178 | `e08ae775e41f2b2cced60234d7d46817490a8826360927b0c1d78c2f8d3c95db` |
| flow_oi | 18,211 | `09caa1a575864a31c0d2855c6fcd351a75587036ad3615564a04ffc9de9780b6` |
| flow_latest | 1 | `4c4a4b17608c15706ae0efdfedcd2f01909c0f3477724759198728245ccebd5b` |

最早事件的基础 payload 哈希从 18:25:47 到最终停止均为 `409f92b4a2ac34952a0faeceeb9ecbb37a1e7e04a3916714f7fbefe971e47833`，后续结果没有重写基础证据。

18:31:41 的只读 API 仍返回最新 100 个事件；BTC 半小时窗口包含 29 根实际蜡烛、16 个事件、24 个分钟末次深度、37 个 OI 点。窗口推进会减少可见点，不等同于数据库数据丢失。

18:31:50 正常关闭只读进程；18:31:52 确认 **8793 监听数为 0**。

## 边界与未验证项

- OI 为 **30 秒采集目标，不是全市场更新保证**。真实运行出现主动配额保护 `1920/1920`、部分轮缺失及零有效 OI 轮保留旧快照；BTC 源时间间隔实际出现 58,419 / 59,453 / 64,351 ms，未补造点。API 的覆盖率、错误和源时间必须保留。
- 后台默认只采 BTCUSDT 深度及核实的对应现货；其他币种深度暂空。深度历史接口按每分钟末次实际观测返回，保留原时间，不插值。
- 系统 Web Push 未接入新订单流事件；此验收未启用 VAPID。原 OI 推送接口保持，不将浏览器内通知等同于关页后后台推送。
- 事件列表 `before` 为排他的 `detectedAt` 游标；同毫秒事件跨分页边界尚无复合游标，不能把单页或简单翻页当作完整事件导出。持久化的事件没有因此删除。
- 7 天保留策略与边界经测试验证，未真实持续运行 7 天；重启缺失路径不补造，实时预热仍需时间。最后一轮全量新目录只验两次 OI 与连接/目录正确性，未重复 6 分钟全市场预热。
- SQLite 已实测；PostgreSQL 共用兼容 SQL 实现，但未连接真实 PostgreSQL 验收。未做生产磁盘容量、长期负载、云部署或跨进程故障长时测试。
